<UserControl x:Class="Wrapp.Controls.PathActionsRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <!-- The app's one way to show an actionable path: single-line ellipsed
         Consolas text (full path in the tooltip) + the standard icon pair
         (E838 open-in-Explorer, E8C8 copy) - identical to the title bar,
         the export prompt, and job-detail path facts. -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="{Binding Path, ElementName=Root}"
                   FontFamily="Consolas" FontSize="11"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   VerticalAlignment="Center"
                   TextTrimming="CharacterEllipsis" MinWidth="0"
                   ToolTip="{Binding Path, ElementName=Root}"
                   Margin="0,0,6,0"/>

        <Button Grid.Column="1" Click="OpenClick"
                Style="{StaticResource ToolbarBtn}" Padding="4,4" Margin="2,0,0,0"
                IsEnabled="{Binding HasRealPath, ElementName=Root}"
                ToolTip="Open in Explorer">
            <TextBlock Text="&#xE838;" FontFamily="Segoe MDL2 Assets" FontSize="11"
                       Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
        </Button>

        <Button Grid.Column="2" Click="CopyClick"
                Style="{StaticResource ToolbarBtn}" Padding="4,4" Margin="2,0,0,0"
                IsEnabled="{Binding HasRealPath, ElementName=Root}"
                ToolTip="Copy path">
            <TextBlock Text="&#xE8C8;" FontFamily="Segoe MDL2 Assets" FontSize="11"
                       Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
        </Button>

        <TextBlock Grid.Column="3" x:Name="CopiedText" Text="Copied!"
                   FontSize="11" Foreground="{DynamicResource ConnectedBrush}"
                   VerticalAlignment="Center" Margin="6,0,0,0"
                   Visibility="Collapsed"/>
    </Grid>
</UserControl>
